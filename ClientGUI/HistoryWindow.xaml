<Window x:Class="ClientGUI.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="История команд" Height="500" Width="800"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="ИСТОРИЯ КОМАНД" FontSize="18" FontWeight="Bold"
                  HorizontalAlignment="Center" Margin="10" Background="#2196F3" 
                  Foreground="White" Padding="10"/>

        <ListBox Grid.Row="1" x:Name="ListHistory" Margin="10" FontFamily="Consolas"/>

        <Button Grid.Row="2" Content="Закрыть" Width="100" Height="35" 
               Click="BtnClose_Click" Margin="10"/>
    </Grid>
</Window>
